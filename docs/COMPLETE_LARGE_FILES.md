# Complete the three large canon files

These are already prepared locally. Upload them via GitHub web UI (or replace the stub Master_Index):

1. Open https://github.com/kuuratsanik/stargate-fifth-race
2. **Add file → Upload files**
3. From your project folder, upload:

| Local path | Repo path |
|------------|----------|
| `artifacts/github_upload_pack/00-canon/Master_Index.csv` | `00-canon/Master_Index.csv` |
| `artifacts/github_upload_pack/00-canon/Master_Production_Bible.md` | `00-canon/Master_Production_Bible.md` |
| `artifacts/github_upload_pack/notion-templates/Episodes.csv` | `notion-templates/Episodes.csv` |

Or unzip `artifacts/stargate-fifth-race-upload.zip` and drag those three files to the correct folders.

Current `00-canon/Master_Index.csv` on GitHub is a **stub** (5 sample rows only). Replace it with the full 200-episode file.

Everything else (templates, scripts, workflows, docs) is already on `main`.
